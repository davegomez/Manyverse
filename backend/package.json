{
  "name": "backend",
  "version": "0.0.0",
  "main": "index.js",
  "scripts": {
    "build": "esbuild src/index.ts --bundle --outfile=out/index.js"
  },
  "devDependencies": {
    "esbuild": "^0.9.0"
  }
}
